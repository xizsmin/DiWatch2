<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diwatch">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <receiver android:name=".MainActivity$MessageReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BROADCAST_ACTION_UPDATE_COUNT"/>
                <action android:name="android.intent.action.BROADCAST_ACTIVITY_RESUMED"/>
                <action android:name="android.intent.action.BROADCAST_ACTION_RESET_COUNT"/>
                <action android:name="android.intent.action.BROADCAST_ACTION_SYNC_COUNT"/>
            </intent-filter>
        </receiver>

        <service android:name=".TimerService"/>

    </application>

</manifest>